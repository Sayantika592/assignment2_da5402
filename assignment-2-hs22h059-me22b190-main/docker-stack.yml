version: "3.9"

services:
  api:
    image: sayantika1234/multimodal-api:latest
    ports:
      - "8001:8000"
    environment:
      - STABILITY_API_KEY=${STABILITY_API_KEY}
    deploy:
      replicas: 4
      restart_policy:
        condition: on-failure